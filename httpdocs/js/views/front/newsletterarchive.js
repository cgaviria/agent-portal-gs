var ViewsFrontNewsletterArchive = Class.extend({
    init: function(){
        jQuery(document).data("ViewsFrontNewsletterArchive", this);

        jQuery(document).ready(function(){

        });
    }
});