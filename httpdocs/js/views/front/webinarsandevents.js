var ViewsFrontWebinarsAndEvents = Class.extend({
    init: function(){
        jQuery(document).data("ViewsFrontWebinarsAndEvents", this);

        jQuery(document).ready(function(){

        });
    }
});